import React from 'react'

interface Props {}

function Topbar(props: Props) {
    const {} = props

    return (
        <div className="">
             Thus is the topbar
        </div>
    )
}

export default Topbar
